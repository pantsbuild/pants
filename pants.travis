#!/usr/bin/env bash

function pythondir {
  python_bin=$(which $1)
  if [ -n "${python_bin}" ]; then
    echo $(dirname ${python_bin})
  fi
}

path=${PATH}:$(pythondir python2):$(pythondir python3)

PATH=${path} LC_ALL=en_US.UTF-8 LANG=en_US.UTF-8 $(dirname "$0")/pants.pex "$@"

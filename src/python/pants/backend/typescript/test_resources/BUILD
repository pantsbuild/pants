# Copyright 2025 Pants project contributors (see CONTRIBUTORS.md).
# Licensed under the Apache License, Version 2.0 (see LICENSE).

# Test resources for TypeScript check integration tests
# Contains package.json and lockfiles generated by actual package managers
#
# IMPORTANT: The package.json file here is used by both:
# 1. Package managers to generate lockfiles (npm, pnpm, yarn)
# 2. Tests via get_package_json_content() to ensure consistency
#
# To regenerate lockfiles:
#   cd test_resources/
#   npm install --package-lock-only
#   pnpm install --lockfile-only
#   yarn install

resources(
    sources=[
        "package.json",
        "package-lock.json",
        "pnpm-lock.yaml",
        "yarn.lock",
        "pnpm_link/**/*",
        "basic_project/**/*",
        "complex_project/**/*",
    ]
)

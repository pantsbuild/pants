<?xml version="1.0"?>
<%!
import os
%>
<!--
=================================================================================================
Copyright 2011 Twitter, Inc.
_________________________________________________________________________________________________
Licensed under the Apache License, Version 2.0 (the "License");
you may not use this work except in compliance with the License.
You may obtain a copy of the License in the LICENSE file, or at:

 http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing, software
distributed under the License is distributed on an "AS IS" BASIS,
WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
See the License for the specific language governing permissions and
limitations under the License.
=================================================================================================
-->

<!-- generated by pants! -->
<project name="${lib.id}" default="compile" basedir=".">
  <import file="${root_dir}/build-support/ant/init.xml"/>

  <!-- TODO(jsirois): consolidate ${lib.id}.sources and allsources -->
  <property name="allsources" value="${" ".join(lib.allsources)}"/>

  <fileset id="${lib.id}.sources" dir="${"${"}root.dir}/src">
  % for path in lib.sources:
    <include name="scala/${path}"/>
  % endfor
  % if lib.java_sources:
    % for path in lib.java_sources:
    <include name="${os.path.relpath(path, os.path.join(root_dir, 'src'))}"/>
    % endfor
  % endif
  </fileset>

  <property name="${lib.id}.scala.sources">
    % for path in lib.sources:
    ${path}
    % endfor
  </property>

  % if lib.java_sources:
  <property name="${lib.id}.java.sources">
    % for path in lib.java_sources:
    ${path}
    % endfor
  </property>
  % endif

  % if lib.resources:
  <property name="${lib.id}.resources">
    % for path in lib.resources:
    ${path}
    % endfor
  </property>
  % endif

  % if lib.deploy_jar:
  <!-- enable dist creating a monolithic deploy jar -->
  <property name="${lib.id}.deployjar" value="true"/>
  % endif

  <property name="compile.name" value="${lib.id}"/>
  <property name="ivy.module" value="${lib.module}"/>

  <import file="${"${"}ant.dir}/module.xml"/>

  % if not lib.exported:
  <target name="_push-single">
    <fail>
      Only bare (non-meta) scala_library modules with a provides key can have their artifacts
      pushed.
    </fail>
  </target>

  <target name="push-single">
    <fail>
      Only bare (non-meta) scala_library modules with a provides key can have their artifacts
      pushed.
    </fail>
  </target>

  <target name="push">
    <fail>
      Only bare (non-meta) scala_library modules with a provides key can have their artifacts
      pushed.
    </fail>
  </target>
  % else:
  <property name="${lib.id}.publish.properties" value="${lib.publish_properties}"/>
  <property name="${lib.id}.publish.repo" value="${lib.publish_repo}"/>
  % endif
</project>

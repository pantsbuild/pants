[package]
name = "local_cas"
version = "0.0.1"
edition = "2021"
authors = [ "Pants Build <pantsbuild@gmail.com>" ]
publish = false

[dependencies]
mock = { path = "../mock" }
clap = "4"
env_logger = "0.9.0"


[package]
name = "fs_util"
version = "0.0.1"
authors = [ "Pants Build <pantsbuild@gmail.com>" ]

[dependencies]
bazel_protos = { path = "../../process_execution/bazel_protos" }
clap = "2"
fs = { path = ".." }

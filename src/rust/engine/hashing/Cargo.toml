[package]
name = "hashing"
version = "0.0.1"
edition = "2018"
authors = [ "Pants Build <pantsbuild@gmail.com>" ]
publish = false

[dependencies]
digest = "0.8"
hex = "0.3.1"
sha2 = "0.8"
serde = { version = "1.0", features = ["derive"] }
serde_test = "1.0"

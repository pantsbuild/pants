# duped from root for testing that task fingerprints change
# when their tools do

jar_library(name = 'checkstyle',
            jars = [
              jar(org = 'com.puppycrawl.tools', name = 'checkstyle', rev = '5.7'),
            ])
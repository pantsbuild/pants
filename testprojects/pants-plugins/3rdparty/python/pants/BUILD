# Copyright 2020 Pants project contributors (see CONTRIBUTORS.md).
# Licensed under the Apache License, Version 2.0 (see LICENSE).

# We intentionally set a bad `modules` value to avoid dependency inference from treating all
# `pants` imports as third-party.
pants_requirement(modules=["pantsbuild.pants"])

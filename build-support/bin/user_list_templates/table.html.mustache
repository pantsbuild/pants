{{!
This template generates markup for embedding in a "Custom HTML / CSS" container in readme.com.
So it cannot contain JavaScript, and its CSS has to be just-so. Getting this to render correctly
can be finicky, so be sure to test out changes!
}}
<div>
    <table>
        {{#org_pairs}}
        <tr>
            <td width="50%">
                <a href="{{ a.website }}" target="_blank">
                    {{#a.image}}
                        <img src="{{ a.image }}" alt="{{ a.name }}">
                    {{/a.image}}
                    {{^a.image}}
                        <span class="org-bigname">{{ a.name }}</span>
                    {{/a.image}}
                </a>
            </td>
            <td width="50%">
                <a href="{{ b.website }}" target="_blank">
                    {{#b.image}}
                        <img src="{{ b.image }}" alt="{{ b.name }}">
                    {{/b.image}}
                    {{^b.image}}
                        <span class="org-bigname">{{ b.name }}</span>
                    {{/b.image}}
                </a>
            </td>
        </tr>
        <tr class="org-name">
            <td colspan="2">
                <br>
                <br>
                <br>
            </td>
        </tr>
        {{/org_pairs}}
    </table>
</div>
<style>
    .markdown-body table tr td {
        border: 0;
        margin: 0;
        padding: 0;
        background-color: white;
        vertical-align:middle;
        text-align:center;
    }
    .markdown-body table tr+tr {
        border: 0;
    }
    .markdown-body .org-name {
        font-size: 18px;
        text-align: center;
    }
    .markdown-body .org-bigname {
        font-size: 48px;
        text-align: center;
    }
    .markdown-body table a[href] {
        text-decoration: none;
        color: black;
    }
</style>

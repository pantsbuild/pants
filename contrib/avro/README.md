Avro code generation support
============================

This Pants plugin generates Java code from Avro schema (.avsc), protocol (.avpr),
and IDL (.avdl) files. Use the `java_avro_library` target with any of those
files as sources.

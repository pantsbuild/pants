# TODO: this would ideally be depended on as a python_protobuf_library(), but that task needs to be
# created, so for now we will just check in this file and its output!

python_library(name='proto-py')
